import 'package:flutter/material.dart';

class BottomComentarioShett extends StatelessWidget {
  const BottomComentarioShett({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 600,
    );
  }
}
